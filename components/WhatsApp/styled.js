import styled, { css, keyframes } from 'styled-components';

export const Icone = styled.img`
    position:fixed;
    width:40px;
    height:auto;
    bottom:10px;
    right:20px;
    cursor:pointer;
`;